[package]
name = "charter-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "charter"
path = "src/main.rs"

[dependencies]
charter-core = { path = "../charter-core" }
clap = { version = "4.5.54", features = ["derive"] }

