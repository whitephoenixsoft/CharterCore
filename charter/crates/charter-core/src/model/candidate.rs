/*
use crate::ids::CandidateId;
use crate::time::Timestamp;

#[derive(Debug, Clone)]
pub struct Candidate {
    pub id: CandidateId,

    /// Semantical information being voted upon
    pub content: String,

    /// The reason why it was proposed
    pub rationale: Option<String>,

    pub created_at: Timestamp,
}
*/
